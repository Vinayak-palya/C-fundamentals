#include<stdio.h>
int main(){
    int a;
    if(3+2%5)
        printf("this works\n");
    if(a=10)
    printf("even this works\n");
    if(2)
    printf(" suprisingly even this works"); 
    return 0;   
}